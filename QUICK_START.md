# 🚀 FaithConnect - Quick Commands

Run these commands from **any directory** in your project!

## 📱 Run the App

### Show available devices:

```bash
./devices.sh
```

### Run on your phone:

```bash
./run.sh ZD222C8W4J
```

### Run on any device:

```bash
./run.sh DEVICE_ID
```

## 🏗️ Build Release APK

```bash
./build-apk.sh
```

APK will be saved to: `faith_connect/build/app/outputs/flutter-apk/app-release.apk`

## 🧹 Clean Project

```bash
./clean.sh
```

---

## 💡 Why These Scripts?

These scripts **automatically navigate to the correct directory** (`faith_connect/`), so you never have to worry about the "No pubspec.yaml file found" error again!

You can run them from:

- `/Users/parthkothawade/Downloads/Projects/FaithConnectHackathon/`
- Or any subdirectory

---

## 🎯 Quick Start

1. **Check devices**: `./devices.sh`
2. **Run app**: `./run.sh ZD222C8W4J`
3. **Build APK**: `./build-apk.sh`

That's it! 🎉
